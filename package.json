{
  "name": "voxai",
  "private": true,
  "version": "0.1.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "build:windows": "npx tauri build",
    "build:windows:cuda": "node scripts/prepare-worker.mjs --cuda",
    "build:all": "node scripts/build-all.mjs",
    "dev:cpu": "npx tauri dev",
    "dev:cuda": "npx tauri dev --features cuda"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "solid-js": "^1.9"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4",
    "@tauri-apps/cli": "^2",
    "tailwindcss": "^4",
    "typescript": "^5.7",
    "vite": "^6",
    "vite-plugin-solid": "^2.11"
  }
}
