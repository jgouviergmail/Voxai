[package]
name = "voxai-llm-worker"
version = "0.1.0"
edition = "2021"
description = "Subprocess worker for local LLM inference (isolates ggml symbols from whisper)"

[dependencies]
llama-cpp-2 = "0.1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
